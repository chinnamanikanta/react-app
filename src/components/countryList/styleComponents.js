// import React from 'react';
import styled from '@emotion/styled';
import tw from 'tailwind.macro';
const CountryListHeader = styled.div`${tw`h-8 border-red-200 w-screen`}`;

export default CountryListHeader;
